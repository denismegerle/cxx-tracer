
#include "maths/vector.h"
#include <iostream>
#include <array>

using namespace gem;

/* mathematical functions on vectors statically */
template <typename T, size_t S>
Vector<T, S> add(const Vector<T, S> &a, const Vector<T, S> &b) {
  return a;
};



// TODO: add, sub, dot, (elementwise) mult, norm, normalize, <outer product>, normal, cross, <reflect>, <refract>
